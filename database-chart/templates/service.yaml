apiVersion: v1
kind: Service
metadata:
  name: {{ include "database-chart.fullname" . | nindent 4 }}
spec:
  selector: {{ include "database-chart.selectorLabels" . | nindent 4 }}
  ports:
  - port: {{ .Values.service.port }}
    name: {{ .Values.service.name  }}
  clusterIP: None